name: dustline-playtest
services:
  - name: server
    type: web
    docker:
      dockerfile: Dockerfile.server
    ports:
      - port: 3000
        protocol: http
    routes:
      - path: /
        port: 3000
    regions:
      - was
    instance_type: free
